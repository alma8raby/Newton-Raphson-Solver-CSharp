# Newton-Raphson-Solver-CSharp
Simple C# implementation of the Newton–Raphson method using the NCalc library. The code is written in a clean, classic style with clear error handling for readable messages. Example: function x*x*x-9 with initial guess 1 (expressions are written as strings for evaluation).
